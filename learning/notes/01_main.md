# notes/01_main.md — `main.go` (リポジトリ直下 / Phase 1 接続テスト)

読んだ対象: `main.go` (リポジトリのルート、50行ほど)

---

## 1. このファイルは何をするか (1行)

このファイルは、多分SlackにHello worldを返すファイルかな
HTTPリクエストってかいてああるから、多分、API関連だとおむ
あとエラーがたくさんかいてある

## 2. 主な型 (struct, interface など)

struct　あとmap
if文　httpリクエストと書いてある部分からまったくわからんくなってるんよね
ここむずい

- os → os、パソコン本体と話す → .envとかをよみにいけるよ！
- log → メッセージを出力 & プログラムを即終了 → fmt.Println だとここでもうダメ、続けたら危険となるので、止まる
- encord/json → jsonという決まった箱を作る開くことをする → Marshal はGoのデータをJson文字列に変換 → Web APIは基本Json
- bytes : byte配列に変形して、それを渡すって感じ
- net/http : Web系全部を渡す
- io : HTTPレスポンスを読む
- fmt : 整形してやる

## 3. 主な関数 (引数・戻り値・1行説明)

os関連わからん、あとhttp関連もちょっと理解できてないな

## 4. 自分が引っかかった所 (具体的に)

token == & channel ==　ってなに？
あと、Marshalっていうメソッドがどこからでてきたのかわかんないし

あとHTTPリクエストの部分は全部わかんない→これって多分あれだよね、暗記とかそーいうものって感じかな？
関数というか、メソッド周りがわかってないね
どのような操作をするのか

そもそもGoのエンジニアが、これのメソッドとかを完璧に覚えているのかどうか聞きたいくらいだね

> 例: 「`os.Getenv` は環境変数を読むらしいけど、なぜ `string` で返ってくる？」

## 5. 他にもありえた選択 (Counter-design)

> 問い: なぜ **`main.go`** (リポジトリ直下) と **`cmd/bot/main.go`** の2つあるか？
> 1つにまとめなかった理由を、自分なりに推測してみる。

これ多分、最初につくったやつだから、そもそもこれいらないかも

## 6. 次に読む人へのアドバイス
