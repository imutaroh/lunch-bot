# notes/03_config.md — `internal/config/config.go`

Loop 2 (前半)。`Config` 型は何者で、どこから読み込まれるかを掴む。

---

## ◆ 比較読み (Comparison Read) — コードを開く前に書く

これは大枠、envファイルのtokenやIDを取得していると思う！
ない場合は、それのエラーを返すって感じかな

> ⚠️ `internal/config/config.go` を開かずに、まず予想を書く。  
> 質より量、勢いで埋める。後で実物と見比べたときの差分が学び。

### Q1. `Config` はどんな情報を持っていそう？

うわー、これはたしか、envファイル系の箱って感じだったかな
大切な情報のをいれていた気がするよ

> ヒント無し。`cmd/bot/main.go` で `Config` がどう使われていたかを思い出す。
> 3つくらい挙げてみる。

(ここに予想)

### Q2. その情報はどこから来る？ (= 誰が `Config` の中身を埋める？)

これは環境変数だね。Getenvがそれを表していると思う

> 例: ファイル? 環境変数? コマンドライン引数? ハードコード?
> なぜそう思うかも一緒に。

(ここに予想)

### Q3. 中身が足りなかったらどうする？

> 例: 落とす? デフォルト値で続行? 警告だけ?
> bot がそれをやる "とき" を想像すると判断しやすい。

中身が足りなかったら、これは必要だよ！って返す感じだね

(ここに予想)

### Q4. `Config` を作る関数のシグネチャを勘で書く

Load() (\*config, error)

って感じか、これはcfgっていう箱に、Configの構造体をいれるって感じだっけ？

> 例: `Load() → ?` みたいに、引数と戻り値を勘で。
> Go の文法は気にしなくていい。

(ここに予想)

---

✏️ ここまで書き終えたら Claude に見せる → 実物を一緒に読みにいく

---

## ◆ 実物を読んだあとに埋める (notes 6 項目)

### 1. このファイルが何をするか (1行サマリ)

envの情報を取得する

### 2. 主な型 (struct, interface)

Configのstruct,

### 3. 主な関数 (引数・戻り値・1行説明)

Load関数でenvを取得している

### 4. 自分が引っかかった所

とくにないかな！これは読めた

### 5. 他にもありえた選択 (Counter-design)

SwithでもIf文いいかと思ったけど、あれだったらどっちか一個しか、requiredできないもんね。それは問題かも

これはエラーの文章をひとつしか取得できていない
だから、もっと詳しくかいて、If文をたくさん書けばいいのかもしれないですね。
Switchだったら、結局ひとつしかできないから無理だね。
||これで繋げるとかはありだったかもね。まーそこまでしなくても、いいのかな

(あとで)

### 6. 次に読む人へのアドバイス (= 未来の自分へ)

これはそこまで難しくないよ！
